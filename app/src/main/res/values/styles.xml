<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/coloreeee</item>
        <item name="colorPrimaryDark">@color/reeed</item>
        <item name="colorAccent">@color/coloreeee</item>
    </style>
    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/reeed</item>
        <item name="colorButtonNormal">@color/reeed</item>
    </style>

</resources>
